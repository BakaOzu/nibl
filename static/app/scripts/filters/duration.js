// Generated by CoffeeScript 1.7.1
(function() {
  'use strict';
  angular.module('niblApp').filter('duration', function() {
    return function(rawDuration) {
      var duration, minutes, seconds;
      rawDuration = Math.ceil(rawDuration / 100) * 100;
      duration = moment.duration(rawDuration);
      minutes = duration.minutes() >= 10 ? duration.minutes() : '0' + duration.minutes();
      seconds = duration.seconds() >= 10 ? duration.seconds() : '0' + duration.seconds();
      return minutes + ' : ' + seconds;
    };
  });

}).call(this);
