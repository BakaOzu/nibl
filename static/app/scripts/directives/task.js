// Generated by CoffeeScript 1.7.1
(function() {
  var __indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  angular.module('niblApp').directive('task', function() {
    return {
      restrict: 'E',
      scope: true,
      replace: true,
      templateUrl: 'static/app/views/partials/task.html',
      controller: function($scope) {
        $scope.isSelected = function(task) {
          return task.isSame($scope.selectedTask);
        };
        $scope.hasHoveredTag = function(task) {
          var _ref;
          return (_ref = $scope.hoveredTag.name, __indexOf.call(task.tags, _ref) >= 0) && !task.isSame($scope.selectedTask);
        };
        return $scope.hasHoveredTagAndSelected = function(task) {
          var _ref;
          if ('url' in $scope.selectedTask && (_ref = $scope.hoveredTag.name, __indexOf.call(task.tags, _ref) >= 0)) {
            return $scope.selectedTask.url;
          }
        };
      }
    };
  });

}).call(this);
