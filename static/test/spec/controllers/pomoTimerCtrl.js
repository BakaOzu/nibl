// Generated by CoffeeScript 1.7.1
(function() {
  'use strict';
  describe('pomoTimerCtrl', function() {
    var ctrl, mockModal, mockTask, pomoTimerCtrl, scope;
    beforeEach(module('niblApp'));
    scope = {};
    pomoTimerCtrl = {};
    mockTask = {};
    ctrl = {};
    mockModal = {};
    beforeEach(inject(function($controller, $rootScope) {
      scope = $rootScope.$new();
      return ctrl = $controller('pomoTimerCtrl', {
        $scope: scope
      });
    }));
    return it('should start timer with 25min', function() {
      return expect(scope.remainingTime).toBe('1500');
    });
  });

}).call(this);
