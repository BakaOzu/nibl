// Generated by CoffeeScript 1.7.1
(function() {
  'use strict';
  describe('sidebarCtrl', function() {
    var ctrl, scope, sidebarCtrl, taskService;
    beforeEach(module('niblApp'));
    scope = {};
    sidebarCtrl = {};
    taskService = {};
    ctrl = {};
    beforeEach(inject(function($controller, $rootScope) {
      var mockTaskService, mockToaster;
      scope = $rootScope.$new();
      mockTaskService = sinon.stub({
        all: function() {},
        post: function(task) {}
      });
      mockToaster = sinon.stub({
        pop: function() {}
      });
      return ctrl = $controller('sidebarCtrl', {
        $scope: scope,
        taskService: mockTaskService,
        toaster: mockToaster
      });
    }));
    it('should be initialized in news mode', function() {
      expect(scope.sidebarMode).toBe('news');
      return expect(scope.selectedTask).toBe({});
    });
    return describe('openNewsView', function() {});
  });

}).call(this);
